from setuptools import setup, find_packages

setup(
    name="diffusion_project",
    version="0.1",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[
        # Add any dependencies here if needed
    ],
)
