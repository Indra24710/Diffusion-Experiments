This experiment uses perceptual loss alone without enforcing normality. It uses K2 test for normality to check if the latents are normal or not. Here the latents are not standardized during the optimization steps and not even at the beginning after getting it from ddim inversion
